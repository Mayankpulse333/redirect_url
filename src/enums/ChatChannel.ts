export enum ChatChannelType {
  CHANNEL = "channel",
  GROUP = "group",
  IM = "im",
  MPIM = "mpim",
}

export enum ChatChannelSource {
  SLACK = "slack",
}

export enum ChatVisibility {
  PRIVATE = "private",
  PUBLIC = "public",
}
